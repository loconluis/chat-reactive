import React from 'react';

export default ({ users }) => {
  return (
    <aside id="sidebar">
      <ul>
        {/*
          users.map(user => 
            (<li
              key={user.id}
            >
              {user.name}
            </li>
          ))
        */}
      </ul>
    </aside>
  );
}



